
---

## 🐳 Сборка и запуск через Docker

Для удобства проверки проект упакован в Docker-образ. Это гарантирует, что проект запустится в точности так, как у разработчика, без необходимости устанавливать Go локально.

### Требования

* Установленный Docker (Docker Desktop).

### 1. Сборка Docker-образа

Выполните эту команду в корневой директории проекта:

``` bash
docker build -t smart-patterns-go:latest .
```

* `-t smart-patterns-go:latest:` Назначает образу имя `smart-patterns-go` и тег latest.

* Указывает, что Dockerfile находится в текущей директории.

### 2. Запуск контейнера
### Запустите контейнер. Поскольку это консольное приложение, оно выполнит все демонстрационные функции и завершит работу.


``` Bash
docker run --rm smart-patterns-go:latest
```
* `--rm`: Указывает Docker'у автоматически удалить контейнер после его завершения, чтобы не засорять систему.
